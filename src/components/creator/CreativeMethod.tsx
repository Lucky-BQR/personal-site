import type { CreatorContent } from '@/types/creator';
export default function CreativeMethod({ items }: { items: CreatorContent['method'] }) { return <section className="spatial-section-compact" aria-labelledby="creative-method-title"><p className="type-meta mb-4" style={{ color: 'var(--color-accent)' }}>CREATIVE METHOD</p><h2 id="creative-method-title" className="type-heading mb-6">创造方法</h2><div className="grid grid-cols-1 sm:grid-cols-3 gap-4">{items.map((item, index) => <div key={item} className="card-base"><span className="card-meta">0{index + 1}</span><p className="card-title mt-4">{item}</p></div>)}</div></section>; }
