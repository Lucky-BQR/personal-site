import type { CreatorContent } from '@/types/creator';
export default function ExplorationSection({ items }: { items: CreatorContent['explorations'] }) { return <section className="spatial-section-compact" aria-labelledby="creator-exploration-title"><p className="type-meta mb-4" style={{ color: 'var(--color-accent)' }}>EXPLORATION</p><h2 id="creator-exploration-title" className="type-heading mb-6">持续探索</h2><div className="flex flex-wrap gap-3">{items.map((item) => <span key={item} className="card-meta rounded-full border px-4 py-2" style={{ borderColor: 'var(--color-border)' }}>{item}</span>)}</div></section>; }
