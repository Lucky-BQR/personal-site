export default function TimelineNode({ year, title, children }: { year: string; title: string; children?: React.ReactNode }) { return <article className="relative pl-8 sm:pl-12 pb-10"><span className="absolute left-0 top-1.5 h-3 w-3 rounded-full border-2" style={{ backgroundColor: 'var(--color-background)', borderColor: 'var(--color-accent)' }} /><time className="type-meta" style={{ color: 'var(--color-accent)' }}>{year}</time><h3 className="type-heading mt-2 mb-2">{title}</h3>{children && <div className="type-body" style={{ color: 'var(--color-textSecondary)' }}>{children}</div>}</article>; }
